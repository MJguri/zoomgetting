<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="team.daddys.zoomgetting.mapper.MainMapper">
    <select id="selectAll" resultType="team.daddys.zoomgetting.domain.Member">
        select * from members
    </select>

</mapper>